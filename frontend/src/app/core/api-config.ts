// URL base da API FastAPI (backend rodando localmente).
// Em desenvolvimento o backend sobe em http://127.0.0.1:8000 (uvicorn).
// Troque aqui se mudar host/porta. (Mantemos simples: uma const, sem environments.)
export const API_BASE_URL = 'http://127.0.0.1:8000';
